import React from "react";
import { Image, SafeAreaView, Text } from "react-native";
import { View } from "react-native";

export default function Library() {
    return <SafeAreaView style={{flex:1}}>
        <View>
            <Text>Kurosaki Ichigo</Text>
        </View>
    </SafeAreaView>
}